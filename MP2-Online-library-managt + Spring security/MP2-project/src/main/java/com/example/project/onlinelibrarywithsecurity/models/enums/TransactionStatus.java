package com.example.project.onlinelibrarywithsecurity.models.enums;

public enum TransactionStatus {

    PENDING, // intermediate state

    SUCCESS, // terminal state

    FAILED // terminal state
}
